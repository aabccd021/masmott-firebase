
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /tweet/{documentId} {

      allow create: if request.auth.uid == request.resource.data.owner
        && request.resource.data.owner is string;
    } 
  }
}
